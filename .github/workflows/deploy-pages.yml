name: Deploy The Inch Park Vault

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }}
      NEXT_PUBLIC_SITE_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Restore published archive data
        run: node scripts/download-published-data.mjs

      - name: Restore image assets
        run: |
          curl --fail --location --output public/escc-logo.png https://edinburgh-south-club-records.djksaunders.chatgpt.site/escc-logo.png
          curl --fail --location --output public/og-vault.png https://edinburgh-south-club-records.djksaunders.chatgpt.site/og-vault.png
          curl --fail --location --output public/og.png https://edinburgh-south-club-records.djksaunders.chatgpt.site/og.png
          touch public/.nojekyll

      - name: Build static site
        run: npm run build:pages

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload site
        uses: actions/upload-pages-artifact@v4
        with:
          path: out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Publish GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
